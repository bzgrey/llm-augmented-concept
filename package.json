{
  "name": "gemini-schedule-demo",
  "version": "1.0.0",
  "description": "llm augmented concept for assignment 3 for 6.104",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/notes.test.js"
  },
  "keywords": ["gemini", "ai", "schedule", "student", "demo", "typescript"],
  "author": "Benjamin Grey",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
